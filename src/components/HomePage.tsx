import LoginForm from "./LoginForm";

const HomePage = () => {
  console.log("rendering HomePage...");

  return (
    <div id="homePage">
      <LoginForm />
    </div>
  );
};

export default HomePage;
